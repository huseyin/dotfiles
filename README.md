Terrawork
=========

Main goal is to create custom development environments that can be run everywhere. Currently, it only focuses on my
problem and cannot be used for another project.

Dependencies
------------

The project has some externals originally written by [@roktas](https://github.com/roktas). Use the following command to
fetch all the changes for those externals:

```sh
git-import
```
